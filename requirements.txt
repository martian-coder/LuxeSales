# ClipNest runtime dependencies
#
# PyGObject provides the GTK3 Python bindings.
# Install via your system package manager (recommended) rather than pip:
#
#   Ubuntu / Debian:  sudo apt install python3-gi python3-gi-cairo gir1.2-gtk-3.0
#   Fedora:           sudo dnf install python3-gobject gtk3
#   Arch:             sudo pacman -S python-gobject gtk3
#
# If you prefer pip (needs build tools & Cairo dev headers):
PyGObject>=3.40.0
